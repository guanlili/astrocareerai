# 复制为 .env.local 并填入真实值（.env.local 已被 gitignore，不会提交）
# 本地用 `bun run dev` 时 Bun 会自动加载 .env.local 到 process.env，
# LLM 调用只在服务端（createServerFn）读取，密钥不进客户端 bundle。

# Qwen ModelStudio (阿里云 DashScope) —— OpenAI 兼容接口
DASHSCOPE_API_KEY=sk-xxxxxxxxxxxxxxxx
# 候选模型，按序尝试、某个不可用（额度/下线/不支持）自动切到下一个（逗号分隔）。
QWEN_MODELS=qwen3-max,qwen-plus,qwen-turbo
# 单模型写法（仅在未设 QWEN_MODELS 时生效）：
# QWEN_MODEL=qwen-plus
# 中国大陆默认端点（不设则用此）：https://dashscope.aliyuncs.com/compatible-mode/v1
# 国际版（新加坡，sk-ws- 开头的 key 用这个）：
# QWEN_BASE_URL=https://dashscope-intl.aliyuncs.com/compatible-mode/v1

# HeyGen 数字人视频面试间（SPEC_video_interview_v1.md §6.1）
# 密钥只在服务端读取（createServerFn），绝不进客户端 bundle。
HEYGEN_API_KEY=
# （可选）云 ASR 密钥，MVP 先用 HeyGen 内置 STT；质量不足时切换。
# 阿里云 DashScope ASR（与 DASHSCOPE_API_KEY 同一账号，可复用，也可单独配）：
# DASHSCOPE_ASR_KEY=

# 生产环境（Cloudflare）请用平台密钥注入，不要写进仓库。
